ReadMe
You are a deeply professional developer. You are going to make deep analysis and resolutions on a large and professional web designed for education with ai. You must perform the changes of the following topics. But first, you have to understand the whole codebase, superficially, but you have to know where you are and where are all the things. Then you have to follow some instructions: you will first analyse all the requested changes or actions, understand them, and connect them(if one needs permissions and the other one is related to them, or maybe one has information for another one); then create an order based on the connections they have. You must then deeply understand each one, what is the objective, analyse the code that will be changed on the codebase and how to perform that change, security risks that may have, you have to cover it; ensure that the change is performed on the most efficient and optimized way, creating new files for cleaner code or reusing some hooks, components or anything that was previously created. After that execute the change, but be very aware of what you are doing, make changes lossless so other external features aren’t lost; perform lint to clean the code and optimize it; check that it works using tests if possible. Ensure it has been completely solved. Then review it, check whether something is missing, and then solve it. Do not ask questions, you are on autopilot. 
Create a plan like from the protocol with subplans for each one where you log everything so you know how to continue and what to do next, so we don't lose track of the changes and everything
Notes:
BEFORE CREATING THE PLAN, DO THE FOLLOWING CHANGES TO COPILOT FILES:
Copilot
IMPORTANT. Make it very clear that copilot must not run commands that might be risky like installing libraries without my permission or so. It cannot use the vscode questions for this if it is on the autopilot mode, because if it is on the autopilot mode, it will auto reply the vscode question itself and it might be dangerous for the user or the web.
If the package is already installed on the repo or somewhere but not on the user, then copilot can install it without the need of question?
Add a new architecture mode to the plans. Architectures are basically the same as the create-plan skill, but in steroids, they demand deep and extensive use of the tools to make a complete and successful result. It is a deep deep deep deep deep deep deep deep plan, where everything must be though on, every possible situation, case, any possible result and must be perfectly handled, a great set of tests must be created to ensure each part, every phase must be ensuring that all tests have been created(both for vitests and e2e with playwright if necessary) to be 100% sure that it works and every case is considered and resolved. Architectures are meant to be around 5 to 30 times longer and more complete than a regular plan, they must be the final boss of plans, leaving everything perfectly logged and documented as well as assessed, clean, errorless, analysing possible threats and resolving them for the future, ensuring everything is centralized when possible to reduce code and improve control over the components and pages. Think of the best possible plan ever made for it, that is the architecture a full bullet-proof mega ultra powerful plan.
You must create a new skill for it, you can’t change the create plan skill, the only thing you must add is that if the required plan is too much, an architecture should be conveyed. Create a new directory for logging the architectures, like on the create plan, and think of a inner structure(like on the plans that have phases, working, review, etc) but for the architecture, it must be huge and powerful, quality is the most important part for the code over quantity, quality code is desired, not a lot of code. Precision and accuracy.
Make the architecture be able to work on different branches, if a huge project is presented, the architecture can create a base branch and from there create branches for every part to ensure they fully work before merging to the base branch, and after finishing all the work, it can merge back to the parent branch of the base branch.
Add to both the architecture and the plan that they both can’t be conveyed on the development/main branch, they must always be done on another branch, a new one or an existing one. Outside of the main or development, plans can be conveyed without moving/creating a new branch, but architecture must always be done on another branch unless the contrary is specified. Add that they must follow the autopilot checklist steps when doing the new branch and so, it is mandatory.
Badges
I want the badges to be divided into 2 groups, depending on who can assign the badge to the student: the teacher or automatically when achieving something. I want a badge that changes depending on the marks achieved: if the student has a 10/10 score on mean, it will have a perfect style. The badge is for good grades, and it appears when achieving an defined threshold that will be defined by the admin institution on the dashboard, the default should be an 8, when reaching 8, they will have this badge(but it can be lost when going under). Also, from 8 to 10 it must be changing the style or the color to indicate the position(for example, it could go from a green color to a gold one or something like that).
Let the badges be created by the teacher, although a default ones can be created. The institution admin should also have a set of badges which will be more general. I want two sets of badges to be: general badges which will be touching a broader and more general aspects, and then the subject badges: each subject will have badges for each student, so the student can be rewarded independently on the different subjects.
The general ones must be handled by the institution admin, but they will be granted automatically, there should be no manual one. The ones from the subjects should be granted both by the teacher and automatically. It would be a great idea that the general ones have some of them that depend on the ones from the subjects, like behaviour or grades for example, and a mean can be computed for them.
let the teacher and institution admin be able to take out a manual badge to a student.
The teacher must be able to handle the badges also from the teacher dashboard and inside each student view.
Teacher Dashboard
On the mis alumnos, the conduct/behaviour should be set as default to 10.
Subject
Two subjects cannot have the same name, course, academic year and classes associated at the same time.
When modifying a subject field like the course or the academic year or the academic period, it must be not only updated there, but to the other things that are related to that. For example, the available classes must be updated on the clases tab of the subject edit overlay(from the three dots) to only appear classes from the new year if the academic year was changed; or if the academic period / year is changed, the subject may pass from not finished to finished, so that must also be checked when changing for the style shown after that.
When editing the subject on the overlay, I want that if changes have been made, but not saved, if clicking outside, it must ask for a confirmation before exiting the edit(because if confirmed, changes will be lost). In fact, I would like to add this to all overlays that have editing/modifying options to prevent the user from misclicking outside the overlay and losing the changes before applying them. Add it to a centralized component so they all have it.
Toggle theme
Toggling the theme using the toggler from the header is sometimes laggy and is not smooth in that sense. Like all the elements are changing at the same time, but they are not doing it smoothly.
Also, when pressing the button of the header toggler, the ball inside the toggler is moving instantly instead of making a transition.
Cursor
Modify the necessary copilot files to make the use of the cursor as pointer mandatory when creating an element that can be clicked. 
Update all the elements that have clicked events so the cursor changes to pointer.

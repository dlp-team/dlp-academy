# Smoke Log — 2026-03-01

## Unit Tests

- Command: `npm run test:unit`
- Result: ✅ Passed (`4` files, `11` tests)
- Notes: Vitest unit baseline is stable after scoping Vitest include pattern to `tests/unit/**`.

## Playwright UI Smoke

- Command: `npx playwright test --ui`
- Result: ✅ UI mode launch initiated; suite discovery confirmed via `npm run test:e2e -- --list`.
- Notes: Current E2E specs are environment-gated and skip without credentials.

## Branding Spec (Phase 2 kickoff)

- Command: `npx playwright test tests/e2e/branding.spec.js --reporter=list`
- Result: ⚠️ Skipped (`1` skipped)
- Notes: Requires `E2E_EMAIL` and `E2E_PASSWORD` environment variables.

## Follow-up Actions

- Re-run `branding.spec.js` and capture pass/fail evidence for Phase 7 admin coverage.

## Phase 2 — Auth and Onboarding Execution (2026-03-02)

- Credential validation command:
	- `$env:E2E_EMAIL` + `$env:E2E_PASSWORD` loaded from `.env`, then `npx playwright test tests/e2e/auth.spec.js --reporter=list`
	- Result: ✅ `1 passed`
- Phase 2 E2E subset command:
	- `npx playwright test tests/e2e/auth.spec.js tests/e2e/auth-onboarding.spec.js --reporter=list`
	- Result: ✅ `3 passed`, ⚠️ `1 skipped` (onboarding completion requires dedicated incomplete-profile user credentials)
- Phase 2 unit validation command:
	- `npm run test:unit`
	- Result: ✅ `6 files`, `15 tests` passed (includes new `useLogin` and `useRegister` suites)

## Remaining Inputs for Phase 2 Closure

- Provide `E2E_ONBOARDING_EMAIL` and `E2E_ONBOARDING_PASSWORD` for a user that still triggers onboarding missing-fields flow.

## Phase 3 — Home and Shared Organization Execution (2026-03-03)

- Added unit suite:
	- `tests/unit/hooks/useHomeContentDnd.test.js`
- Targeted validation command:
	- `npm run test:unit -- tests/unit/hooks/useHomeContentDnd.test.js`
	- Result: ✅ `1` file, `4` tests passed.
- Full unit regression command:
	- `npm run test:unit`
	- Result: ✅ `7` files, `19` tests passed.
- Notes:
	- Phase 03 started with non-flaky DnD logic coverage.
	- Phase 02 onboarding completion remains blocked only by missing onboarding-specific E2E credentials.

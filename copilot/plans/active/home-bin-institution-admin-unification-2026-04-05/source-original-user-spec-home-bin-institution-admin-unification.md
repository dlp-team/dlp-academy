<!-- copilot/plans/active/home-bin-institution-admin-unification-2026-04-05/source-original-user-spec-home-bin-institution-admin-unification.md -->
# Source Input - Original User Spec

ReadMe
You are a deeply professional developer. You are going to make deep analysis and resolutions on a large and professional web designed for education with ai. You must perform the changes of the following topics. But first, you have to understand the whole codebase, superficially, but you have to know where you are and where are all the things. Then you have to follow some instructions: you will first analyse all the requested changes or actions, understand them, and connect them(if one needs permissions and the other one is related to them, or maybe one has information for another one); then create an order based on the connections they have. You must then deeply understand each one, what is the objective, analyse the code that will be changed on the codebase and how to perform that change, security risks that may have, you have to cover it; ensure that the change is performed on the most efficient and optimized way, creating new files for cleaner code or reusing some hooks, components or anything that was previously created. After that execute the change, but be very aware of what you are doing, make changes lossless so other external features aren’t lost; perform lint to clean the code and optimize it; check that it works using tests if possible. Ensure it has been completely solved. Then review it, check whether something is missing, and then solve it. Do not ask questions, you are on autopilot.
Create a plan like from the protocol with subplans for each one where you log everything so you know how to continue and what to do next, so we don't lose track of the changes and everything
Scrollbar space
Previously you left some space for the scrollbar to make a smooth transition so when necessary it did not move the whole page. The issue is that you also left a margin at the left, where no scrollbar is going to appear, I want you to take out this margin on the left for a scrollbar and leave it as the web, or, if it is necessary to make the page be centered make it have the same color so it is not visible.
Selection Mode
On the selection mode for the home page, I want you to copy an aspect of the bin. On the bin section, when the user is in selection mode and at least one element is selected, then the unselected elements lose saturation, so the selected ones are focused. If you can try to unify both the selection mode from the bin section to the selection mode on the other sections of the home.
Bin Section
On the bin section of the home mode, I want to make some improvements:
When the user is in grid mode and it presses an element, the whole background blurs and two borders appear. I don’t want that to happen, make the card scale to be more focused with a transition and then let the options appear.
In the list mode, when pressing an element the options appear at the bottom of all the elements and they have a completely different style than the ones on the grid mode. Make the options appear just below the selected element with a transition(move the elements from below so they are also visible when the options are visible) and use the same style as the grid mode.
Institution Admin
On the settings of the institution admin, let the inst admin be able to set up the start and end of the ordinary and extraordinary periods so these ones are the default option when creating a course.
On the settings of this page, add the courses order, so the automatic transfer of the courses can be done, there must appear a settings section where the different courses of the institution appear(make them nonduplicated) and then make them able to be dragged and moved to the desired position, but try to know where to set them as default, the order normally is 2º Bachillerato <- 1º Bachillerato <- 4,3,2,1º ESO <- 6,5,4,3,2,1º Primaria or something related to that.
Also, on the settings add the options for enabling and disabling the different automatic tools that there are on the web, in case the admin does not want some.
On the customization tab, when making the preview full screen, the header is still over the full-screen preview, so it is not fully visible, fix that.
Also, on the customization tab, the topics and the documents, quizzes and the rest of the elements inside the topic are not appearing exactly as the web, in fact, they are a completely different thing. Make a deep analysis on the subject, topic, quizzes, exam, study guide and these pages to exactly know how it works and make an exact exact replica for the preview. The bin section on the home preview is not the exact replica of the real section, fix that also. Also add the exact header on the preview.
Also, make sure that when changing the colors of the customization, the corresponding colors of the preview also change at that moment, so the inst admin can clearly see the changes. Also, when selecting a color and making it active, highlight the zones that affect that color inside the preview by using borders or squares with margin that indicate these zones so the inst admin can see it more easily.
Make the institution admin able to delete users on the users tab.
Also, inside the userviews, it is not getting the profile picture of the user, make sure the profile picture is being used with firestorage. In addition to this, don’t use emojis, use svgs or react symbols. Make that the classes that were associated to a teacher have a special section of previous classes or past classes on the user view for the teacher and for the student.
Modals and similar
When a modal appears, or an overlay with similar form and dimensions to configure, edit or create something, I want to have a generalized style for all of them, let’s unify them so they have the same style and it is easier to maintain and to make changes to them. The first thing is that they must be between the header and the bottom of the screen(this is already done on the home page modals or some of them, so see it from there); I want to also have the option to leave the modal when pressing outside of it(if the modal can be closed, if it is not required to be filled), but there must be a confirmation of exiting if something on the modal has been modified or selected, to prevent accidental losses because of this; make the width and height be handled from there, like make this component be used with parameters like a class or a function that then customize the modal(if it is better).

Other arrangements for the institution admin dashboard not from the customization tab are:
On the academic configuration, add the start and end for each period inside the academic year so they are the predefined ones when creating the courses(so it is faster and easier for the institution admin when creating a course, to already have filled those fields with the predefined values, although it must be able to change them).
In the classes section add a filter option for each academic year to filter by course or teacher.
In the courses section, make that as default, only the current courses are visible instead of todos.
The Institution ID is being duplicated below the Panel de Administración.
Settings
On the "Mostrar selector de tema en el encabezado", make it be disabled by default for new users.
Bin
On the bin section of the home page, make the selection mode have the same ui from the rest of the sections of the home page, to maintain the same location, position and style and be a more smooth change between sections, don't change the options, only the aesthetic to match the one from the other sections of the home page.
Profile
For admin or institution admin roles, the profile should be more like a general statistics rather than the badges, the stats of the notes.
Scrollbars
Change all the normal scrollbars so they use the new modern scrollbars like the global one.
Sign in
Sign in must enforce email verification before creating the new user and access the web.
Selection Mode
When the selection mode is active and more than 6 elements have been selected, when dragging, only 6 of them make the transition and do correclty the ghost mode for the dragging, but from that number on, the last n-6 elements(being n the number of elements selected and n > 6), are not making the transition and ghost mode.
Copilot
A new file or directory must be created for copilot to know how to create determinate objects or parts of the code. For example, the overlays should always be between the header(if there is any on the page) and the bottom of the screen. I think there is a component for this and that all from the home comply with this, but it must be ensured that copilot always maintains this for the new ones when creating. Another one is the scrollbars, it must use the same style as the global scrollbar, and note that the global scrollbar has as its top part the header(it does not go over the header), this must also be ensured. 
Make a deep analysis on what components or web elements are widely used and repeated with a certain style for the web and include them.
Copilot files like copilot-instructions and AGENTS.md (and more) must be modified so it uses these specific style decisions.
Create like an index inside the new folder for copilot to see faster if a change that it is going to do has something to do with the task that it has assigned. If for example it has to do a new overlay create modal or something where an scrollbar is needed, it must be interested by those 2 elements on the index and it should go to the corresponding documentation of those elements to understand what is the protocol or the proper way on the web to do it.

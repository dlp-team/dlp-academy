<!-- copilot/plans/todo/autopilot-plan-scrollbar-execution-2026-04-12/sources/source-scrollbar-fix-reference-autopilot-plan-scrollbar-execution-2026-04-12.md -->
ReadMe
You are a deeply professional developer. You are going to make deep analysis and resolutions on a large and professional web designed for education with ai. You must perform the changes of the following topics. But first, you have to understand the whole codebase, superficially, but you have to know where you are and where are all the things. Then you have to follow some instructions: you will first analyse all the requested changes or actions, understand them, and connect them(if one needs permissions and the other one is related to them, or maybe one has information for another one); then create an order based on the connections they have. You must then deeply understand each one, what is the objective, analyse the code that will be changed on the codebase and how to perform that change, security risks that may have, you have to cover it; ensure that the change is performed on the most efficient and optimized way, creating new files for cleaner code or reusing some hooks, components or anything that was previously created. After that execute the change, but be very aware of what you are doing, make changes lossless so other external features aren’t lost; perform lint to clean the code and optimize it; check that it works using tests if possible. Ensure it has been completely solved. Then review it, check whether something is missing, and then solve it. Do not ask questions, you are on autopilot. 
Create a plan like from the protocol with subplans for each one where you log everything so you know how to continue and what to do next, so we don't lose track of the changes and everything
Notes:
BEFORE CREATING THE PLAN, DO THE FOLLOWING CHANGES TO COPILOT FILES:
Copilot
add to the plan creation protocol and skill and the AUTOPILOT_EXECUTION_CHECKLIST that commits and pushes must be done inside the correspondent branch that it is being worked on, because copilot is always forgetting it, even when we have skills and protocols about it. 
Selection Mode
When elements are selected on the selection mode, I want that they can be dragged and dropped on other elements, like with individual elements, and have the same behaviour as individual ones. But they must be dragged all at once, when dragging them, all the selected elements should make a transition moving towards the one where the dragged was performed, and they must be mounted below this one so all move with the dragged. They must all be moved and on the ghost mode of the drag and drop, all the selected ones, not the one where dragging started. So when dragging  a selected one, the other ones that are also selected must disappear and only the ghost elements should remain(and they should be with the dragged one), but make a transition so it can be seen that the selected ones are also being dragged with the other one. Think of google drive, when selecting several ones and dragging, all of them are moved to the one where dragging started. 
Also, when making an action on all of them (like dropping for moving) and a confirmation is required, make the confirmation have the names of the elements that are going to be moved, unless there are too many (more than 5), when the confirmation will only display the number of elements and the first 5 with then three dots or “+” signs.
Also, make that if using ctrl + click on an element, selection mode is automatically started with that element selected, and inside selection mode, if using ctrl+click on the selection mode, make it access the element(if it is a subject, go inside the subject to the Subject page; if it is a folder, go inside of the folder, but leave the selection mode on); if using ctrl+shift+click, make like the control+shift+click on google drive or other apps that it selects all the elements between the last selected one and the new selected one.
Also, after performing an action(move to a folder, delete, etc), let the undo the whole action on all the selected/affected elements, because when pressing the undo notification, only the first or the last one(i don’t remember which one) is being undone instead of all of them.
Bin Section
On the bin section of the home mode, I want to make some improvements:
When the user is in grid mode and it presses an element, it is scaled, but if I press again on the same element, the temas or the elements badge like turns invisible and then goes ok, but it is not smooth and it disappears for an instant. 
Also, don’t make the background change the opacity when pressing an element on the list mode, take that out(I mean when pressing an element, the rest have a lower opacity or lower saturation on the rest, i don’t want that to happen, let them remain the same).
Institution Admin Dashboard
On the institution admin dashboard on the customization tab on the preview window, several improvements have to be conveyed:
	Take out from the preview header the vista profesor selector, not the vista docente/estudiante that is on top next to the computer/tablet/mobile preview buttons, the one that is actually inside the preview. That is going to be handled from the button on the top(the vista docente/estudiante).
	You have to create mock up data for the teacher and the student view, because they will have different elements than the admin, they must be independent, only for the preview purposes. Make that the admin also has a student and teacher dashboard that is mocked so when pressing the preview it navigates there, because they don’t currently exist, it goes to the home page.
	Solve that when a color is selected, and the parts affected by the color are highlighted with the rectangles, when pressing over the color square card of the color card, it is not currently showing the selector, it is only showing it when pressing that part of the color card(the one that shows the current color) and when it is not active/selected.
	Also, changing the color isn’t changing it at the same time on the preview, it must be changing at the same time to see the preview, but without saving it, only when pressing save.
Scrollbar
I need to fix the global scrollbar for this web application. All the contents for this fix where provided on the SCROLLBAR_FIX.md that claude generated to help us.


ReadMe
You are a deeply professional developer. You are going to make deep analysis and resolutions on a large and professional web designed for education with ai. You must perform the changes of the following topics. But first, you have to understand the whole codebase, superficially, but you have to know where you are and where are all the things. Then you have to follow some instructions: you will first analyse all the requested changes or actions, understand them, and connect them(if one needs permissions and the other one is related to them, or maybe one has information for another one); then create an order based on the connections they have. You must then deeply understand each one, what is the objective, analyse the code that will be changed on the codebase and how to perform that change, security risks that may have, you have to cover it; ensure that the change is performed on the most efficient and optimized way, creating new files for cleaner code or reusing some hooks, components or anything that was previously created. After that execute the change, but be very aware of what you are doing, make changes lossless so other external features aren’t lost; perform lint to clean the code and optimize it; check that it works using tests if possible. Ensure it has been completely solved. Then review it, check whether something is missing, and then solve it. Do not ask questions, you are on autopilot. 
Create a plan like from the protocol with subplans for each one where you log everything so you know how to continue and what to do next, so we don't lose track of the changes and everything
Copilot
This is not about the web but about copilot behaviour. Change the copilot-instructions, agents.md and the skills if necessary for the following: You have to ensure that it uses github to commit and push the changes every some time to log the changes regularly. It already has that, but it does not use it, it has to use it regularly to log the changes.
Selection mode
Selection mode on the home page should not be blue, because it is not aesthetic, it must be like the selection mode on the bin tab, after pressing, you can leave the blue color. Nevertheless, change the ui when the selection mode is on(the seleccionados, crear carpeta, mover a …, mover a papelera and so), they must have a more simple, organize and elegant approach, make the seleccionados badge be on the same level as the salir de la selección at the left part.
Also, the selection mode on the bin does not allow to select multiple elements, only one. Make here a similar ui that the one you are going to implement on the other selection mode so the style is maintained.
Settings
On the settings, on the change password, when I have logged in with google, it does not let me “change my password”, which makes sense, but I should be able to set a password linked to the email with an authentication step with a verification process using email(this should also be used when logging in with the email instead of google). This way, a user can enter with google and with its personal password.
Add here the option to be remembered when enter
Dashboards
Add pagination to all the dashboards where there are a list of students or teachers or users or institutions or whatever that may require a lot of requests.
Institution Admin
On the institution admin dashboard, change the search bar on the users tab so it is after the security access section(below).
Also, on the customization tab of this dashboard, you have correctly set a preview with mock examples for teacher and student view, but we can improve it more: make the preview be able to be set full screen, so it can be exactly seen as it would be(maintain at the left the options that can be changed, but make them be collapsable to have full full screen). Also, the mock preview only shows the first page on the manual tab, but it does not show any other section like the usage, courses, shared, bin and so(they are visible but when pressing it does not change). I would also like to be able to go inside the folders and subjects and see the topics(inside the subjects) and inside the topics the exams, tests, quizzes, material, formulas, study guides and so. All this exactly as it is on the web, it must be an exact replica, but that has dynamic elements that can change(the colors, name of institution, logo, icon).
On the institution admin dashboard, on the customization section, I want that there is an exact, exact replica of the web on the preview but with mock data(mock subjects, folders, topics, documents, everything) so the institution admin can check how are the different configurations affecting the web. But it must be an exact replica, how can we do this? Should we use the home component and so? Should we create a specific mockstudent@domain and mockteacher@domain for each institution that is an account that only the institution admin can enter on that preview with like a live preview of these mocks? Make an audit and decide the best path.
Also, I don’t know why but the different tabs section on the institution dashboard has like a scrollbar, I mean on the Usuarios Cursos y Clases Personalización div(the one that is used to press on one of those options and go to that tab), take the scrollbar out.
Scrollbar
I would like that all pages use some scrollbar styles, I want to have one that is like the present one, but also one that does not have the triangles at the upper and bottom part, only the middle bar is visible and it does not have a lot of opacity until hovered or pressed.
I want the scrollbars to be integrated on the app. What I mean by that is that when you pass from a non-scrollbar page to a scroll bar page the whole app should not shift to the left, I want it to be integrated so these changes are smooth and not detected.
Element issues
When a teacher tries to delete a shortcut that has been deleted from the owner and its on ghost mode, it cannot,  it does nothing, it should be moved to the bin.
Also, when pressing the delete button on a shared element where the user is not owner, it also does nothing, but I want that element to be unshared with the user and then the shortcut deleted without passing through the bin. 
Also, when editing a subject where the user is owner and pressing save, it says that it appears a message on the background home page saying that it could not be saved, the overlay modal is already there, but if you go out, the subject was correctly saved so I don’t know what is happening, because for one part, the error/alert should not appear on the background because it is not visible and then it said it did not worked but it did, so make a deep analysis on this.
Real tests
Convey real tests using the e2e to ensure that firestore works and everything is working as expected. You can use E2E_SHARED_FOLDER_ID, E2E_FOLDER_ID, E2E_SUBJECT_ID and E2E_TOPIC_ID that belong to the E2E_OWNER_EMAIL and E2E_OWNER_PASSWORD from the .env

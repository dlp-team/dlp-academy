// copilot/explanations/codebase/.gitignore.md

### Changelog
#### 2026-04-06
- Added `!.env.example` to allow committing a safe environment template while keeping `.env` and `.env.*` ignored.
- Preserved all secret-protection ignore rules for real environment files.

---
#### 2026-03-08
- Added `tmpclaude-*` pattern to ignore all files and folders matching this prefix, per user request.
- Preserved all previous ignore rules and file structure.

---
#### Summary
The `.gitignore` now excludes all `tmpclaude-*` files and folders, preventing them from being tracked by git. No errors detected after the change. All previous ignore rules remain intact.
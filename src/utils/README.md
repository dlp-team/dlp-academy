# Utilities & Constants

This directory contains **pure functions** and **static data**. 

## 🧊 What goes here?
- **Constants:** Configuration arrays that don't change (e.g., list of countries, list of subject icons, color palettes).
- **Helpers:** Formatting functions (e.g., `formatDate`, `calculateProgress`).

## Files
- `subjectConstants.js`: Contains `ICON_MAP`, `COLORS`, `EDUCATION_LEVELS`.
- `profileConstants.js`: Contains `COUNTRIES` list.

*Note: Code here should never contain JSX/React components.*